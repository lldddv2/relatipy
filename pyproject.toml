[build-system]
requires = ["uv_build>=0.12.0,<0.13"]
build-backend = "uv_build"

[project]
name = "relatipy"
version = "1.0.0"
description = "Numerical tools for relativistic geometry"
readme = "README.md"
requires-python = ">=3.11"
license = "GPL-3.0-only"
license-files = ["LICENSE"]
authors = [
    { name = "lldddv2" },
]
dependencies = []
